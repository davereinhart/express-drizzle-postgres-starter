{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "API server",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "preLaunchTask": "buildTask",
      "program": "${workspaceFolder}/build/server.js",
      "outputCapture": "std"
    }
  ]
}
